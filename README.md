# Documents of FWAT package

## How to compile this document

First, install `sphinx` and `sphinx_rtd_theme` in your local Python environment.

```
pip install sphinx sphinx_rtd_theme
```

Then compile the static web pages with the following command

```
make html
```